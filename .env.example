# Copy to .env or .env.local and set values.
# When VITE_API_URL is set, the app uses the backend API (JWT auth + CRUD).
# When unset, the app runs in local-only mode (localStorage).

VITE_API_URL=http://127.0.0.1:8000

# Optional: Gemini API key for AI features
GEMINI_API_KEY=
